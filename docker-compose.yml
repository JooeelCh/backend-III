version: "3.8"

services:
  app:
    build: .
    image: adoptme:latest
    container_name: adoptme-app
    ports:
      - "8080:8080"
    env_file:
      - .env
    command: npm start
    volumes:
      - .:/app
      - /app/node_modules